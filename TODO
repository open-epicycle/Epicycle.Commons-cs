 * Epicycle.Commons.Binary:
   * BinaryUtils
     * Add Float32/64 Serialization/deserialization
     * Add flip endianity to uints
   * BinaryReaderUtils
     * Add Float64 deserialization
     * Get rid of the temp stream in ReadFloat32
     * Add pascal buffer read
     * Create string version to byte[] methods
   * BinaryWriterUtils
     * Add Float32/64 Serialization
     * Add pascal buffer write
     * Create string version to byte[] methods
 * Epicycle.Commons.Reporting:
   * PeriodicReport: Report(..) is ambiguous
   * An easy way to write/append SimpleReport to file
   * IStatisticsReporter: should have a consistant order (if it's not like this now)
   * Dummy reporters implementation
   * UTs
 * Epicycle.Commons.Collections:
   * Some of the AsReadOnlyList should be ToReadOnlyList